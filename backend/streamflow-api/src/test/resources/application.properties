# ─────────────────────────────────────────────────────────────────────────────
# Test-profile application.properties for streamflow-api
#
# Overrides the main application.properties for all tests that use
# @SpringBootTest without an explicit Cassandra container.
#
# SPEC-18: Cassandra auto-configuration is excluded here so that existing
# integration tests (StreamApiIT, AlertAndCbWebSocketIT) continue to run
# without a Cassandra container. HistoryControllerIT starts its own
# CassandraContainer and overrides the exclusion via @SpringBootTest(properties).
# ─────────────────────────────────────────────────────────────────────────────

spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.cassandra.CassandraAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.cassandra.CassandraDataAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.cassandra.CassandraRepositoriesAutoConfiguration

# Cassandra contact-points still defined to avoid binding errors
spring.cassandra.contact-points=localhost
spring.cassandra.port=9042
spring.cassandra.local-datacenter=datacenter1
spring.cassandra.keyspace-name=streamflow
spring.cassandra.schema-action=none
